

const initialState = {
    userName:'',
    password:''
};
//login reducer
const loginReducer = (state = initialState, action) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};

export default loginReducer;
